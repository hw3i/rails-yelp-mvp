<%= simple_form_for @restaurant, class: 'form-inline' do |f| %>
  <%= f.input :name %>
  <%= f.input :category %>
  <%= f.input :address %>

  <%= f.submit "Create A Restaurant", class: "btn btn-primary" %>
<% end %>
